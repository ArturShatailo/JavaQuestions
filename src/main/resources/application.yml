# Bot configuration
bot:
  TOKEN: <5625550512:AAFSdO15T_EWhiwz9kMlX-HYslCBh1M7Bw4>
  USERNAME: <JavaQuestions_bot>
# Server configuration
server:
  PORT: 5000

# Spring Boot configuration
spring:
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/java_questions
    username: postgres
    password: Postgresql
  # JPA properties
  jpa:
    hibernate:
      ddl-auto: update # When you launch the application for the first time - switch "none" at "create"
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true

